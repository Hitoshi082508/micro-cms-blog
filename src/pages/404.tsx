import React from 'react'
import styled from 'styled-components'

export const Custom404: React.FC = () => {
  return (
    <main className="main">
      <p>ページがありません。</p>
    </main>
  )
}

export const Custom404Page = styled(Custom404)``

export default Custom404Page
